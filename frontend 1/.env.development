# Backend base URL used for file uploads.
# Uploads go directly to the backend, bypassing the Vite proxy, because
# the Vite 7 proxy resets multipart/form-data connections from browser fetch().
# CORS is enabled on the Express server so :3002 → :5000 cross-origin works.
VITE_BACKEND_URL=http://localhost:5000
